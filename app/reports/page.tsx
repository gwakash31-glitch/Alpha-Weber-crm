import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="reports" title="Reports" subtitle="Daily, weekly, monthly, yearly, sales, lead, client, project, task, and meeting reports with export and print UI." />;
}
