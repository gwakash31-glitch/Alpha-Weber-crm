import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="Roles and permissions" subtitle="Owner, Admin, Manager, Sales, Support, Marketing, Employee, Viewer, permission matrix, custom roles, and role assignment UI." />;
}
