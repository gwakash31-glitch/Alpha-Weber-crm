import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="productivity" title="Meetings" subtitle="Meeting dashboard, list, details, schedule meeting, notes, participants, timeline, upcoming, completed, cancelled, and attachments." />;
}
