import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="pipeline" title="Sales pipeline" subtitle="Complete kanban pipeline with stages, drag-and-drop placeholders, lead cards, deal cards, expected revenue, probability, priority, tags, activities, and timeline." />;
}
