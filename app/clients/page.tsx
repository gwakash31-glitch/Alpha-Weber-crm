import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="clients" title="Client management" subtitle="Complete client CRM frontend with company information, contact person, phone, email, address, industry, projects, payments summary, invoices summary, timeline, documents, meetings, communication history, and attachments." />;
}
