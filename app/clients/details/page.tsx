import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="clients" title="Client details" subtitle="Client detail workspace for profile sidebar, information cards, history, meetings, notes, documents, tags, and future API-ready state handling." />;
}
