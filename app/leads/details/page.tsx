import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="leads" title="Lead details" subtitle="Detailed lead workspace prepared for future Supabase records with notes, timeline, activity feed, attachments, owner assignment, tags, and follow-up sections." />;
}
