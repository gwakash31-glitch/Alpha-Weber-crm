import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="leads" title="Lead management" subtitle="Complete lead CRM frontend with list, details, add/edit placeholders, import/export, advanced filters, sorting, pagination, bulk actions, scoring, priority, owners, notes, timeline, attachments, and follow-up sections." />;
}
