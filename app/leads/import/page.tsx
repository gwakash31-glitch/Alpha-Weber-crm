import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="leads" title="Import and export leads" subtitle="CSV import, export controls, validation placeholders, bulk actions, loading, empty, and error states for lead records." />;
}
