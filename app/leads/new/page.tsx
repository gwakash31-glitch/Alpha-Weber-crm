import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="leads" title="Add lead" subtitle="Create-lead interface using reusable CRM table, toolbar, filter, status, priority, timeline, and confirmation patterns." />;
}
