import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="Security" subtitle="Login history, active sessions, connected devices, audit logs, API keys, access tokens, 2FA setup, and security activity." />;
}
