import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="Payments" subtitle="Payment dashboard, transactions, payment history, refund UI, billing history, and payment methods." />;
}
