import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="Admin panel" subtitle="Admin dashboard, users, companies, organizations, system health, activity logs, usage analytics, feature management, plans, subscriptions, billing, tickets, feedback, announcements, and maintenance mode." />;
}
