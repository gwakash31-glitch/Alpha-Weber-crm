import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="Subscriptions" subtitle="Pricing, current plan, upgrade, downgrade, renewal, invoices, and usage limits." />;
}
