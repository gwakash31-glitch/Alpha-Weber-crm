import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="productivity" title="Task management" subtitle="Task dashboard, list, details, create/edit/delete UI, assigned members, due dates, priority, labels, statuses, checklist, attachments, comments, timeline, search, filter, sort, pagination, and bulk actions." />;
}
