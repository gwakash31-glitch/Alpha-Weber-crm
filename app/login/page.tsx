import { AuthForm, AuthLayout } from '@/components/auth/auth-components';
export default function Page(){return <AuthLayout title="Welcome back" subtitle="Sign in with a secure, validation-ready interface prepared for future Supabase Auth."><AuthForm mode="login"/></AuthLayout>}
