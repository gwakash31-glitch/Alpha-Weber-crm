import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="Files and storage" subtitle="File manager, folder view, grid view, recent files, shared files, trash, upload UI, preview UI, and storage usage." />;
}
