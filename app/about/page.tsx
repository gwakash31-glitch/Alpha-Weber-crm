import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="final" title="About Alpha Weber CRM" subtitle="Final frontend phase completion overview, changelog, keyboard shortcuts, quick actions, and product information." />;
}
