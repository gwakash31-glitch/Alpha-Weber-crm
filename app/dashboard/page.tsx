import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="dashboard" title="Executive CRM dashboard" subtitle="Welcome header, company summary, KPI cards, revenue, leads, clients, deals, tasks, meetings, monthly performance, pipeline, activities, calendar, notifications, quick actions, search, and filters." />;
}
