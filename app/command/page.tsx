import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="final" title="Final UI utilities" subtitle="Command palette, global search, keyboard shortcuts, quick actions, feedback dialog, about page, and changelog." />;
}
