import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="integrations" title="Integrations" subtitle="Integration cards for Supabase, Firebase, AI providers, Google services, Meta, payments, messaging, SMTP, Slack, Zapier, and webhooks." />;
}
