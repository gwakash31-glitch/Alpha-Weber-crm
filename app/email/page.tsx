import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="Email module" subtitle="Inbox, compose, templates, sent, drafts, trash, and email preview frontend." />;
}
