import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="Invoices" subtitle="Invoice dashboard, list, details, create, edit, preview, quotation, estimate, and PDF preview UI." />;
}
