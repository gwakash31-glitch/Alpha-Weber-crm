import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="WhatsApp module" subtitle="Chat list, conversation UI, contact list, template messages, broadcast UI, and media gallery." />;
}
