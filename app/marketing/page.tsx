import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="Marketing" subtitle="Campaign dashboard, list, details, email campaign UI, WhatsApp campaign UI, landing pages, and analytics." />;
}
