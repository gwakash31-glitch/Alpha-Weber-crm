import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="Notifications" subtitle="Notification center, history, unread notifications, notification preferences, and application states." />;
}
