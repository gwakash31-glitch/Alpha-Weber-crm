import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="AI assistant" subtitle="AI dashboard, chat interface, prompt library, conversation history, suggestions, meeting summary, lead summary, email generator, and proposal generator UI." />;
}
