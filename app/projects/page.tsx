import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="productivity" title="Project management" subtitle="Project dashboard, list, details, timeline, milestones, progress tracking, assigned members, deadlines, files, notes, activity, kanban view, and list view." />;
}
