import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="Team management" subtitle="Team dashboard, member list, member profile, invite/edit/remove member UI, departments, teams, designations, organization chart, and member activity." />;
}
