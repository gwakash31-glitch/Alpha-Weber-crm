import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="analytics" title="Analytics" subtitle="Revenue, lead conversion, sales growth, task progress, project progress, client growth, team performance, top performers, monthly trends, activity overview, charts, graphs, comparisons, and trends." />;
}
