import { WorkspacePage } from '@/components/workspace/workspace-components';

export default function Page() {
  return <WorkspacePage kind="admin" title="Workspace settings" subtitle="General, workspace, branding, company information, language, timezone, currency, appearance, theme, security, notifications, email preferences, backup, API, and developer settings." />;
}
